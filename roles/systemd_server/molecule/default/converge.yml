---
- name: Converge
  hosts: all
  gather_facts: false
  tasks:
    - name: Include blueprints.vault.systemd_server with caller-supplied variables
      ansible.builtin.include_role:
        name: blueprints.vault.systemd_server
      vars:
        vault_server_image: "example/vault:latest"
        vault_server_admin_secret: "dummy-not-a-real-secret"
